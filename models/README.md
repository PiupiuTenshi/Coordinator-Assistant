# Models

Phase 3 sẽ lưu model đã fine-tune vào:

- `models/symptom_ner/`
- `models/triage_classifier/`

Các thư mục này hiện chỉ chứa placeholder. Model thật được sinh sau khi chạy:

```bash
python training/train_ner.py
python training/train_triage.py
```
